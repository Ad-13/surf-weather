import weatherForecast from '@src/features/weatherForecast/WeatherForecast/weatherForecastSlice';
import citySearch from '@src/features/weatherForecast/CitySearch/citySearchSlice';

const reducers = {
  weatherForecast,
  citySearch,
};

export default reducers;
